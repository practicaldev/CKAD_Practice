apiVersion: v1
kind: Pod
metadata:
  namespace: os # kubectl create namespace os 
  name: busybox
  labels:
    app: busybox
    uses: command
spec:
  containers:
  - image: busybox
    name: busybox
    command: ['sh','-c','echo $MESSAGE && sleep 3600']
    env:
    - name: MESSAGE
      valueFrom:
        secretKeyRef:
          name: busybox-secret
          key: message
